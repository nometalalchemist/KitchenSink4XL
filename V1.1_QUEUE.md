# V1.1 Queue — deferred findings from the pre-beta rounds (2026-09-05)

Triage of the LOW findings the combined pre-beta fix wave did not take (all
HIGHs and MEDIUMs from both final-round reports were fixed; the cheap honest
LOWs rode along). One line each; report IDs reference
`20260905_xl_prebeta_fresheyes.md` (FE) and `20260905_xl_prebeta_destroyer.md`
(D) in Agent Results.

- **FE L-4** — document (or optionally honor) row visibility in
  query_range/export_range/aggregates: filter-hidden rows are silently
  included today and nothing in the docstrings says so.
- **FE L-5** — sheet-name lint for Excel-UI-forbidden-but-openable names:
  case-duplicate pairs (Base/BASE), the reserved "History", trailing spaces;
  warn or refuse.
- **FE L-6** — Unicode normalization note (or NFC fold) in find_cells /
  replace_cells: NFD-stored Korean is invisible to an NFC query.
- **FE L-7** — import_data: validate the encoding name even when the source
  is inline (unknown codec like "utf-99" is accepted silently today).
- **FE L-8** — polish batch: `columns: [[]]` leaks an unhashable-type
  message; text_rotation refusal dumps a 46-element allowed set; negative
  freeze offset silently clamped; Windows device names (CON) accepted as
  out_file; com_set_sparkline action=list ignores its sheet param.
- **FE L-9** — validate references check returns passed:true while
  unknown_sheet_references is non-empty; passed should reflect the finding.
- **D L-3** — a read-only target file refuses with "is open in Excel";
  a read-only attribute (files off old media) deserves its own message.
- **D M-3 residual** — the H3 over-refusal: a workbook whose only hazard is
  a legacy note's vmlDrawing*.vml refuses ALL file-tier edits even though
  openpyxl round-trips the note (renaming the anchor to commentsDrawing);
  needs a VML content classifier plus merging the two anchor spellings into
  one renumbered family. Conservative direction (over-refusal, no data at
  risk), hence deferred.
- **D M-4 residual (docs)** — consider surfacing the "prev only covers
  server-made mutations" limit in the README/onboarding, not only in the
  manage_backups docstring and the recover-workbook workflow.
