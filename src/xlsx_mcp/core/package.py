"""core/package.py: XlsxPackage, the raw-bytes SpreadsheetML package.

STUB (Phase 0). Built in Phase 2 (DESIGN Section 2.4, PLAN reuse ledger 1.2):
raw-bytes dict, lazy lxml trees, mark_dirty, byte-identical passthrough of
untouched parts, append-only sharedStrings/styles discipline, atomic
validated save. The KS4W package pattern with SpreadsheetML index discipline.

Nothing here yet; the module imports cleanly so the package tree is complete.
"""

from __future__ import annotations
