"""core/calc.py: the formula / calc layer.

STUB (Phase 0). Built in Phase 2/3 (DESIGN Section 4, PLAN reuse ledger 1.2),
prototyped in the Phase 1 spike: the _xlfn./_xlpm. normalization shim,
dynamic-array plumbing, fullCalcOnLoad injection, cached-value labeling
(cached | computed | formula | absent), and the `formulas`-lib fallback
wrapper with coverage reporting. No pure-Python engine computes formulas at
fidelity; COM is the fidelity path (DESIGN principle 3, the honest calc story).

Nothing here yet; the module imports cleanly so the package tree is complete.
"""

from __future__ import annotations
